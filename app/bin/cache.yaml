framework:
  cache:
    # service: cache.redis
    app: cache.adapter.redis
    default_redis_provider: 'redis://localhost'

    pools:
      redis.cache:
        provider: 'redis://localhost'
        default_lifetime: 3
        tags: true
